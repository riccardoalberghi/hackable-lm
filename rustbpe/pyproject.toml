[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "hackable-lm-rustbpe"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

[tool.maturin]
module-name = "_hackable_lm_tokenizer"
features = ["pyo3/extension-module"]
